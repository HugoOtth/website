#!/usr/bin/env sh

# Run Prettier on the whole repo to keep formatting consistent
pnpm run format

# Re-stage any files that Prettier modified so they land in this commit

# Optionally, add lint --fix if you want:
pnpm lint:fix